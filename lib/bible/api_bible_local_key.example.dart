/// Copy to `api_bible_local_key.dart` and set [apiBibleLocalKey] for local/org builds,
/// or leave empty and pass `--dart-define=API_BIBLE_KEY=...` when building.
/// End users are never prompted for a key in the app.
const String apiBibleLocalKey = '';
